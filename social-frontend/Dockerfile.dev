# Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies first for caching
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite's default dev port
EXPOSE 5173

# Run with host flag to allow access outside container
CMD ["npm", "run", "dev", "--", "--host"]
